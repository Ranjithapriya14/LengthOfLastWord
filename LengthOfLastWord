class Solution {
    public int lengthOfLastWord(String s) {
        s=s.trim();
        int l;
        l=s.lastIndexOf(' ');
        if(l==-1){
            return s.length();
        }
        return s.length()-l-1;
    }
}
